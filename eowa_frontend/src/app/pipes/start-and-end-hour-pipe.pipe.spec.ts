import { StartAndEndHourPipePipe } from './start-and-end-hour-pipe.pipe';

describe('StartAndEndHourPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StartAndEndHourPipePipe();
    expect(pipe).toBeTruthy();
  });
});